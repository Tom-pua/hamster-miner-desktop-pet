# 桌宠矿工架构说明

## 目标

当前版本采用“可配置的模块化单体”，继续保持 Windows 自带 PowerShell/WPF 即开即玩，同时把高频变化的内容、数值、规则、用例、存档和界面拆开。没有引入微服务、数据库、插件系统或第三方运行时。

依赖方向固定为：

```text
config  ←  domain  ←  app  ←  DeepDesk.ps1（组合根）  →  ui
                     ↑                ↓
                   infra（由组合根调用）
```

## 模块职责

- `config/content.json`：宠物、技能、稀有度、装备槽、套装与贴图映射。加内容时优先只改这里。
- `config/balance.json`：生命成长、掉率、养成价格、攻击系数、速率限制、自动保存等数值。调平衡时优先只改这里。
- `config/Config.ps1`：加载配置并校验重复 ID、必需集合和权重。
- `domain/Rules.ps1`：生命、伤害、宠物成长、孵化价格、回挖比例和装备比较等纯规则；不允许访问 WPF、文件或全局状态。
- `app/GameApplication.ps1`：技能购买/重置、自动择优换装等用例；可以修改传入状态，但不显示界面、不写文件。
- `infra/Storage.ps1`：默认值合并、旧存档迁移、原子保存和备份；不包含玩法判断。
- `ui/*.xaml`：桌宠 HUD、冒险手册和统一游戏弹窗。更换主题无需触碰数值公式。
- `DeepDesk.ps1`：唯一组合根，连接系统输入、游戏状态、模块和 WPF 控件。

## 常见扩展方式

### 新增宠物

在 `content.json` 的 `pets` 增加唯一名称、品质、效果类型、基础数值与说明。通用数值型效果会自动参与等级成长；全新机制需在组合根补充触发时机，并把计算公式放进 `domain`。

### 新增技能

在 `skills` 增加唯一 `id`、路线、层级、上限、数值和说明。技能树会按层级正序生成。若是全新触发机制，只在组合根接事件，在 `domain` 添加纯计算函数。

### 调整数值

优先编辑 `balance.json`，不要把新数字直接写进事件处理器。稀有度、装备池或掉率属于内容配置，编辑 `content.json`。

### 改界面主题

只修改 `ui/overlay.xaml`、`ui/panel.xaml`、`ui/dialog.xaml`。已有 `x:Name` 是运行时控件契约，改名时必须同步主脚本并补测试。

### 升级存档结构

先提高 `balance.json` 的 `saveVersion`，再在 `Invoke-GameStateMigrations` 增加向前迁移逻辑。迁移必须保留旧资源与进度，且可重复执行。

## 防回退检查

运行 `自检.ps1` 会执行旧存档迁移测试、真实 WPF 加载、玩法回归、关键奖励保存及连续两次原子写入。运行 `python -m unittest discover -s tests -v` 会额外检查内容唯一性、权重、模块依赖边界和 XAML 控件契约。
